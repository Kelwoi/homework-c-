<Window x:Class="GmailClient.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Login" Height="200" Width="400">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0">
            <TextBlock Text="Email:" />
            <TextBox x:Name="EmailTextBox" Width="300" />
        </StackPanel>

        <StackPanel Grid.Row="1" Margin="0,10,0,0">
            <TextBlock Text="App Password:" />
            <PasswordBox x:Name="PasswordBox" Width="300" />
        </StackPanel>

        <Button Grid.Row="2" Margin="0,10,0,0" Width="100" Content="Login" Click="Login_Click" HorizontalAlignment="Left" />
    </Grid>
</Window>