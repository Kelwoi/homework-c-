<Window x:Class="AsyncFactorialApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Async Factorial Generator" Height="250" Width="400">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
            <TextBox x:Name="InputTextBox" Width="200" Margin="0,0,10,0" />
            <Button Content="Calculate Factorial" Click="CalculateButton_Click" Width="150"/>
        </StackPanel>

        <ListBox x:Name="ResultsListBox" Grid.Row="2"/>
    </Grid>
</Window>
